#!/usr/bin/env bash

# TODO better to get this path from fvm itself
export FLEX_HOME="$(fvm home)"

# TODO find a way to make this this file's path
alias fvm-restart="source $(fvm restart)"

echo "fvm-restart run successfully"
echo $FLEX_HOME